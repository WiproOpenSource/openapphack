
==== BROADCAST =================================================================

<%
	broadcasts.each { broadcast ->
		println broadcast.text
	}
%>
Running OPENAPPHACK server ${openapphackVersion} on vertx ${vertxVersion}

================================================================================
